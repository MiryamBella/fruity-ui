import { useState } from "react";
import { useParams } from "react-router-dom";
import {Tab, Tabs} from "react-bootstrap";


export default function FruitsInfoView() {
    const {id} = useParams();

    return <div>
<h2>info</h2>
    </div>
}
