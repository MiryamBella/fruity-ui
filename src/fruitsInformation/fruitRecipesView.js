import { useState } from "react";
import { useParams } from "react-router-dom";
import {Tab, Tabs} from "react-bootstrap";


export default function FruitsRecipesView() {
    const {id} = useParams();

    return <div>
<h2>recipes</h2>
    </div>
}
